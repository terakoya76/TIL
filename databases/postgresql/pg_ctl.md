# pg_ctl

which
```bash
$ ls /usr/lib/postgresql/10/bin/
clusterdb   dropuser           pg_basebackup   pg_ctl      pg_receivewal   pg_rewind       pg_upgrade  psql
createdb    initdb             pgbench         pg_dump     pg_recvlogical  pg_standby      pg_waldump  reindexdb
createuser  oid2name           pg_config       pg_dumpall  pg_resetwal     pg_test_fsync   postgres    vacuumdb
dropdb      pg_archivecleanup  pg_controldata  pg_isready  pg_restore      pg_test_timing  postmaster  vacuumlo
```
